{
  "name": "codenames",
  "version": "3.0.0",
  "description": "El juego de codenames en español",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd react-ui/ && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.17.1",
    "react-ga": "^3.0.0",
    "socket.io": "^2.3.0"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react"
  ]
}
